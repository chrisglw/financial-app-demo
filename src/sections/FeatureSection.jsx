function FeatureSection({ title }) {
  return (
    <section className="section section-feature">
      <h2>{title || 'Feature Section'}</h2>
      <p>TODO: text copy + phone-framed mockup, alternating layout</p>
    </section>
  )
}

export default FeatureSection
